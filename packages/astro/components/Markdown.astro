---
import { renderRichtext } from '@handover/core';

interface Props {
  content: string;
}

// The HTML is built from the validated tree, so `set:html` cannot emit what a tier disallows.
const html = renderRichtext('default', Astro.props.content);
---

<Fragment set:html={html} />
