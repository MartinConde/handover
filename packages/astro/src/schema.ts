// `drizzle.config.ts` points here; Better Auth's tables are in so both land in one migration.
export {
  account,
  activity,
  cronState,
  drafts,
  locks,
  media,
  pathReservations,
  rateLimit,
  session,
  settings,
  user,
  verification,
} from '@handover/core';
